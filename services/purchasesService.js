/**
 * purchasesService.js — REMOVED
 *
 * This app is completely free. RevenueCat has been removed.
 * Ad-free experience is now available via rewarded ads (see adFreeService.js).
 * AI credits are managed via aiCreditsService.js.
 *
 * This file is kept as an empty stub to avoid breaking any lingering imports
 * during migration. Safe to delete once all references are cleaned up.
 */

export const initPurchases = async () => {};
export const getOfferings = async () => [];
export const purchasePackage = async () => false;
export const restorePurchases = async () => false;
